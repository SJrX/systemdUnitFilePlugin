[Unit]
# A short human readable title of the unit.
Description=

[Timer]
# How often the timer should trigger (see: https://www.freedesktop.org/software/systemd/man/systemd.time.html#) for syntax
OnCalendar=daily
# Unit to activate, if not specified will default to ${NAME}.timer)
Unit=

[Install]
# Ensures that the unit is
WantedBy=timers.target

